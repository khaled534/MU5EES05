from max import max_value
from min import min_value
from var import variance

# Example array
tableau = [2, 4, 6, 8, 10]

# Using breakpoints for debugging
import pdb; pdb.set_trace()

print("Max:", max_value(tableau))
print("Min:", min_value(tableau))
print("Variance:", variance(tableau))
